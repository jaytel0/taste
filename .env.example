# Local pipeline credentials.
# For the highest-fidelity default pipeline, set both direct provider keys:
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Convenience alternatives. If either of these is set, it can run all models
# through a single gateway key instead of direct provider keys.
OPENROUTER_API_KEY=
AI_GATEWAY_API_KEY=
