# Claude Notes

Read `AGENTS.md` first. It has the setup commands, repo map, and credential
guardrails.

For this repo, keep the hosted web app OpenRouter-OAuth only. If work needs
individual OpenAI/Anthropic keys, build that as local tooling on top of
`packages/ai`, not as hosted `apps/web` UI or public API.
